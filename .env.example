# ============================================================
# NFC Standee SaaS — Environment Variables
# Copy this file to .env.local and fill in your actual values.
# NEVER commit .env.local to version control.
# ============================================================

# ------------------------------------------------------------------
# DATABASE (Supabase PostgreSQL + Prisma)
# ------------------------------------------------------------------

# Connection string for Prisma Client (uses connection pooler in prod)
DATABASE_URL="postgresql://postgres.ncvftaqggxsdvgxidgbd:M.H@ikal.07@://supabase.com"

# Direct connection string — used ONLY for Prisma migrations (bypasses pooler)
DIRECT_URL="postgresql://postgres.ncvftaqggxsdvgxidgbd:M.H@ikal.07@://supabase.com"

# ------------------------------------------------------------------
# GOOGLE MAPS / PLACES API
# ------------------------------------------------------------------
# Used in the browser for Places Autocomplete widget.
# Restrict this key to HTTP referers in the Google Cloud Console.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="AIza..."

# ------------------------------------------------------------------
# ADMIN
# ------------------------------------------------------------------
# Secret token to protect POST /api/admin/generate-batch.
# Use a long, random string. Example: openssl rand -hex 32
ADMIN_SECRET="natanhaikal10jutapertama"

# ------------------------------------------------------------------
# APP
# ------------------------------------------------------------------
# The public base URL of your deployment (no trailing slash).
# Used to generate QR code URLs like: https://your-domain.com/c/UMKM-001-A7X9
NEXT_PUBLIC_BASE_URL="https://your-domain.com"
